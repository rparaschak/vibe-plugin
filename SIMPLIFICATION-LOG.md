# Simplification Log

Constraints removed from vibe's templates and commands, and what happened after.

**Why this file exists:** instructions accumulate; models improve. A constraint that compensates for a weakness the model no longer has is pure context cost. Practice (from Learn Harness Engineering L4/L12): periodically disable a suspect constraint, observe real runs, and if quality holds — delete it and record it here. An entry is a success, not an apology.

**Entry format:** date | constraint removed | where it lived | why it existed | evidence quality held | follow-up.

| Date | Constraint | Lived in | Why it existed | Evidence | Follow-up |
|---|---|---|---|---|---|
| 2026-07-14 | **Runtime branching on project shape** (spec-or-not, worktree-or-not, QA-or-not, Platform→BE→FE ordering) | v1 universal commands (`implement.md` 174 lines) | One file had to serve every project shape at execution time | Replaced by generation-time specialization: presets stamp lean linear commands, zero dead branches. Both presets ≤80 lines; implement variants byte-identical past header; 3 live self-test sessions PASS incl. a forced-failure andon (D9/D15, `.workspace/migration/selftest/`) | Confirm on live-project validation (Phase 5) |
| 2026-07-14 | **Two orchestration skills** (`team-communication` + `team-orchestration`) kept separate; **idle-run rule stated twice** | v1 `skills/` | Historical split; each co-referenced the other | Merged into one `vibe-team-protocol` kernel template; duplicated idle-teammate rule single-sourced (ORCH's re-ping-then-verify sequence, D6). 70 lines, cross-check restored 2 real losses, cold review Accept | — |
| 2026-07-14 | **Consolidation rule restated in 3 places** + **stance paragraphs** re-narrating each agent's own skill | `architect.md`, orchestration skill, `build-flow.md`; `critic.md`, `qa-engineer.md` | Near-verbatim duplication accumulated across files | Consolidation rule single-sourced in the kernel; restated stance paragraphs deleted; single-consumer skills `vibe-manual-testing` and `vibe-critique` inlined into `qa-engineer`/`critic` templates (D8). All agents Accept within ≤70-line budget | — |
| 2026-07-14 | **adopt's `CHECKLIST.md` contract** — a hardcoded required-item list the project had to conform to | v1 `CHECKLIST.md` + `/vibe:adopt` | Onboarding needed a fixed setup contract | Absorbed into the builder's `vibe-task-ledger` checklist (`behavior/verification/state/evidence`), generated per-project and inverted in direction (harness conforms to project). `CHECKLIST.md` deleted; build-harness passing, 19/19 rows on self-test | — |
| 2026-07-14 | **"Never commit mid-run"** (whole-plan commit only at Finalize) | v1 `implement.md` (disposition I6) | Avoided partial commits inside a run | Contradicted the design doc's leaf evidence = commit hash + test ref → replaced by **per-block commits** (D9): block commit → review cites the hash → leaf evidence complete → Finalize commit closes ledger/learnings only. Self-test `/implement`: block commit 07bc48e cited by review Accept, Finalize commit 6a64bc8 | — |
| 2026-07-14 | **LLM-judged flow-quality validation** ("validate guarantees are preserved") | v1 `build-flow.md:54-66,83-88` | Soft check that a generated flow kept load-bearing guarantees | Replaced by **structural inheritance** (kernel sections are fixed skeleton text the builder cannot omit) + a **mechanical lint** (roles/lenses resolve on disk, gates present, blessed opt-outs, single artifact, no runtime-conditional prose). build-flow passing, targeted re-review Accept | — |
